# JLUDating 日志 - 2026-04-06

## 本次修复汇总（全部 Bug 已修复 + 安全加固 + 免费部署配置）

### 🔴 崩溃级 Bug（已修复）

1. **prisma/schema.prisma** — 删除重复的 `datasource db` 块（Prisma 7.x 不支持 url 在 schema 里，连接配置在 prisma.config.ts）
2. **matching.service.ts:126** — `partnerProfile` 逻辑反转修正（`isA ? match.userB.profile : match.userA.profile`）
3. **admin.controller.ts** — 添加 `@UseGuards(AuthenticatedGuard)` 保护所有 admin 路由
4. **admin.module.ts** — 导入 `AuthModule`
5. **authenticated.guard.ts** — 将 `import * as jwt from 'jsonwebtoken'` 替换为 `JwtService`（@nestjs/jwt），消除隐式依赖
6. **auth.module.ts** — exports 加入 `JwtModule`，使 AdminModule 中 AuthenticatedGuard 能注入 JwtService

### 🟡 中级 Bug（已修复）

7. **main.ts** — CORS `origin: true` + `credentials: true` 组合无效，fallback 改为明确的 localhost 地址列表
8. **admin.service.ts** — `getWhitelist()` 从数据库查询，移除硬编码测试域名（@163.com）
9. **api/auth.ts** — refresh 改用 apiClient（统一 baseURL），添加并发 refresh 保护（_isRefreshing），同步更新 Zustand store（动态 import），`window.location.href` 改为 `window.location.replace`
10. **match-lobby.tsx** — 问卷总题数从 38 改为 52（与后端一致），修复 `/match-result` 链接为 `/match/result`
11. **use-auth-store.ts** — 移除 `session: getCachedSession()` 初始值（消除 localStorage key 竞争），完全依赖 zustand/persist

### 🛡️ 安全加固

12. **register.dto.ts** — 增强密码策略：12位以上 + 大小写字母 + 数字 + 特殊字符
13. **env.validation.ts** — 生产环境强制检查 JWT_SECRET 长度 >= 32，禁止默认值
14. **app.module.ts** — 增强 Rate Limiting：全局 100次/分钟，认证接口 5次/分钟
15. **auth.controller.ts** — 细粒度限流：sendCode 3次/分钟，login/register 5次/分钟
16. **frontend package.json** — 添加 dompurify 依赖用于 XSS 防护
17. **register.tsx** — 前端密码验证规则与后端同步更新

### 其他

- **package.json (api)** — `start:prod` 路径改为 `dist/src/main`（与编译输出一致）

### 编译验证

- `prisma generate` ✅
- `nest build` ✅（0 errors）
- `tsc --noEmit`（前端）✅（0 errors）

### 冒烟测试状态

- 本地无 Docker → Redis + PostgreSQL 未运行 → 无法完整测试
- 需要先启动 Docker Desktop，然后 `docker compose up -d`

### 部署文档

**生产部署**：
- `DEPLOYMENT.md` — 腾讯云 ECS 完整部署步骤、Nginx 配置、PM2 进程管理

**免费部署（新增）**：
- `FREE_DEPLOYMENT.md` — Vercel + Railway 免费部署指南
  - Supabase PostgreSQL（免费 500MB）
  - Upstash Redis（免费 10K 命令/天）
  - Railway（免费 $5 额度/月）
  - Vercel（免费 100GB 带宽/月）

**配置文件（新增）**：
- `jludating-api/.env.production.example` — 生产环境配置模板
- `jludating-api/RAILWAY.md` — Railway 部署说明
- `jludating-web/vercel.json` — Vercel 部署配置
- `jludating-web/.env.production.example` — Vercel 环境变量模板
