[build]
builder = "nixpacks"
buildCommand = "cd jludating-api && npm install && npm run build && npx prisma generate"

[deploy]
startCommand = "cd jludating-api && node dist/src/main.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[variables]
NODE_ENV = "production"