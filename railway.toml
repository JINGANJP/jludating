[build]
builder = "nixpacks"
buildCommand = "cd jludating-api && npm install && npm run build"

[deploy]
startCommand = "cd jludating-api && npm run start:prod"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
